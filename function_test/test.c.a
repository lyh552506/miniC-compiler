main:
    addi sp, sp, -32
    sd ra, 24(sp)
    sd s0, 16(sp)
    addi s0, sp, 32
    fcvt.w.s .4, .3 , rtz
    sw .4, -20(s0)
    lw .6, -20(s0)
    fcvt.s.w .7, .6
    ret
    ld ra, 24(sp)
    ld s0, 16(sp)
    addi sp, sp, 32
    ret
