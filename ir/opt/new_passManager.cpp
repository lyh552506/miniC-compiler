#include "New_passManager.hpp"

template <typename Scope>
void _PassManager<Scope>::DecodeArgs(int argc, char *argv[]) {
    
}

template <typename Scope> void _PassManager<Scope>::RunOnLevel(OptLevel level) {
  if (level == O0)
    return;
  if (level == O1) {
  }
}